<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.reach.mapper.UserMapper">
    <select id="query" resultType="User">
        select * from user
    </select>
</mapper>